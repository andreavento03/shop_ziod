---
import { readdirSync } from 'node:fs';
import { join } from 'node:path';

let photos: string[] = [];
try {
  const dir = join(process.cwd(), 'public', 'clothes');
  photos = readdirSync(dir)
    .filter((f: string) => /\.(jpg|jpeg|png|webp)$/i.test(f))
    .sort()
    .map((f: string) => `/clothes/${encodeURIComponent(f)}`);
} catch {
  photos = [];
}
---

<section id="collezione" class="bg-white py-24 md:py-36 px-6">
  <div class="max-w-7xl mx-auto">

    <div class="text-center mb-16">
      <p class="text-turquoise text-xs tracking-[0.35em] uppercase mb-4 font-light">Le Nostre Creazioni</p>
      <h2 class="text-navy" style="font-family: 'Playfair Display', serif; font-size: clamp(2rem, 4vw, 3rem);">
        La Collezione
      </h2>
      <div class="w-12 h-px bg-turquoise mx-auto mt-6"></div>
    </div>

    {photos.length === 0 ? (
      <p class="text-center text-gray-400 font-light py-20">La collezione è in arrivo...</p>
    ) : (
      <div id="gallery-grid" class="columns-1 sm:columns-2 lg:columns-3 xl:columns-4 gap-3">
        {photos.map((photo, i) => (
          <div
            class="break-inside-avoid mb-3 overflow-hidden rounded-lg cursor-pointer group relative"
            data-photo={photo}
            data-index={String(i)}
          >
            <img
              src={photo}
              alt={`Creazione ${i + 1}`}
              loading="lazy"
              class="w-full object-cover transition-transform duration-500 group-hover:scale-105"
            />
            <div class="absolute inset-0 bg-navy/0 group-hover:bg-navy/20 transition-all duration-300 rounded-lg flex items-center justify-center">
              <svg class="w-8 h-8 text-white opacity-0 group-hover:opacity-100 transition-opacity duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0zM10 7v3m0 0v3m0-3h3m-3 0H7" />
              </svg>
            </div>
          </div>
        ))}
      </div>
    )}
  </div>
</section>

<!-- Lightbox -->
<div
  id="lightbox"
  class="fixed inset-0 z-[200] items-center justify-center"
  style="display: none; background: rgba(5, 15, 35, 0.96);"
>
  <div class="relative flex items-center justify-center w-full h-full px-16 py-12" id="lb-backdrop">
    <!-- Close button -->
    <button
      id="lb-close"
      class="absolute top-5 right-5 text-white/60 hover:text-white transition-colors z-10"
      aria-label="Chiudi"
    >
      <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>

    <!-- Prev -->
    <button
      id="lb-prev"
      class="absolute left-4 md:left-6 text-white/50 hover:text-white transition-colors z-10"
      aria-label="Precedente"
    >
      <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 19l-7-7 7-7" />
      </svg>
    </button>

    <!-- Image -->
    <img
      id="lb-img"
      src=""
      alt=""
      class="max-w-full max-h-full object-contain rounded shadow-2xl"
      style="max-height: 85vh; max-width: 80vw;"
    />

    <!-- Next -->
    <button
      id="lb-next"
      class="absolute right-4 md:right-6 text-white/50 hover:text-white transition-colors z-10"
      aria-label="Successivo"
    >
      <svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5l7 7-7 7" />
      </svg>
    </button>

    <!-- Counter -->
    <div class="absolute bottom-5 left-1/2 -translate-x-1/2 text-white/40 text-sm font-light" id="lb-counter"></div>
  </div>
</div>

<script define:vars={{ photos }}>
  const lightbox = document.getElementById('lightbox');
  const lbImg = document.getElementById('lb-img');
  const lbCounter = document.getElementById('lb-counter');
  let current = 0;

  function openLightbox(idx) {
    current = idx;
    lbImg.src = photos[current];
    lbCounter.textContent = `${current + 1} / ${photos.length}`;
    lightbox.style.display = 'flex';
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.style.display = 'none';
    document.body.style.overflow = '';
    lbImg.src = '';
  }

  function navigate(dir) {
    current = (current + dir + photos.length) % photos.length;
    lbImg.src = photos[current];
    lbCounter.textContent = `${current + 1} / ${photos.length}`;
  }

  // Gallery click
  document.getElementById('gallery-grid')?.addEventListener('click', (e) => {
    const card = e.target.closest('[data-index]');
    if (card) openLightbox(parseInt(card.dataset.index));
  });

  // Controls
  document.getElementById('lb-close')?.addEventListener('click', closeLightbox);
  document.getElementById('lb-prev')?.addEventListener('click', () => navigate(-1));
  document.getElementById('lb-next')?.addEventListener('click', () => navigate(1));

  // Close on backdrop click
  document.getElementById('lb-backdrop')?.addEventListener('click', (e) => {
    if (e.target === e.currentTarget) closeLightbox();
  });

  // Keyboard
  document.addEventListener('keydown', (e) => {
    if (lightbox.style.display !== 'flex') return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') navigate(-1);
    if (e.key === 'ArrowRight') navigate(1);
  });
</script>
